Мой первый проект на hexlet
The install command from the Makefile file installs the Lodash library.
  The lint command automatically checks code for errors.  
  The lint-fix command automatically checks your code for errors and fixes them.  
